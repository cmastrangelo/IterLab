# Base URL of the IterLab API, as reachable *from the browser*.
# For the default docker-compose setup the API is published on the host at :8000.
# NEXT_PUBLIC_* values are inlined at build time.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
